spring:
  datasource:
    url: jdbc:mysql://192.168.99.100:3306/test_prod
    username: test_prod_user
    password: admin #Should be encrypted or just passed as the EV
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false